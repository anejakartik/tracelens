name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install ruff pytest
          if [ -d sdk ]; then pip install -e ./sdk; fi
      - name: Lint
        run: ruff check . || true   # tolerant until first real code lands
      - name: Test
        run: pytest -v tests/ || true   # tolerant until first real tests land
